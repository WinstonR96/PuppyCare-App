<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/profile"
            android:checked="true"
            android:icon="@mipmap/ic_profile"
            android:title="Perfil" />

        <item
            android:id="@+id/donacion"
            android:checked="true"
            android:icon="@mipmap/ic_donacion"
            android:title="Donación" />

        <item
            android:id="@+id/help"
            android:icon="@mipmap/ic_help"
            android:title="Ayuda" />

        <!-- Otra sección -->
        <item
            android:id="@+id/otra_section"
            android:title="Configuracion">
            <menu>
                <item
                    android:id="@+id/logout"
                    android:icon="@mipmap/ic_logout"
                    android:title="Cerrar sesion" />
            </menu>
        </item>
    </group>

</menu>